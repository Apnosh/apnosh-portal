/**
 * Default loading state for owner /dashboard routes — a full-screen mvp shell
 * skeleton (see components/mvp/mvp-loading). Owner pages render their own
 * MvpShell, so the loading state must match that, not the old desktop column.
 */
export { default } from '@/components/mvp/mvp-loading'
