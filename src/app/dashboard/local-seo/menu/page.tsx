/**
 * /dashboard/local-seo/menu — edit the food menu shown on the
 * Google Business Profile listing.
 */

import MenuEditor from './menu-editor'

export const dynamic = 'force-dynamic'

export default function MenuPage() {
  return <MenuEditor />
}
