/**
 * /dashboard/insights/coming-up — the schedule as a page (owner 2026-09-14). Chrome-free via the
 * /dashboard/insights prefix in the layout's MVP allowlist; the component owns its shell.
 */
import ComingUpPage from '@/components/mvp/coming-up-page'

export default function Page() {
  return <ComingUpPage />
}
